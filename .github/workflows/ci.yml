name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo run typecheck --filter='!@doceu26/desktop'
      - run: pnpm turbo run build --filter='!@doceu26/desktop'

  backend:
    name: Backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Build
        working-directory: services
        run: |
          for mod in $(go work edit -json | jq -r '.Use[].DiskPath'); do
            echo "Building $mod..."
            (cd "$mod" && go build ./...)
          done
      - name: Test
        working-directory: services
        run: |
          for mod in $(go work edit -json | jq -r '.Use[].DiskPath'); do
            echo "Testing $mod..."
            (cd "$mod" && go test ./...)
          done
      - name: Vet
        working-directory: services
        run: |
          for mod in $(go work edit -json | jq -r '.Use[].DiskPath'); do
            echo "Vetting $mod..."
            (cd "$mod" && go vet ./...)
          done

  deploy:
    name: Deploy to EC2
    needs: [frontend, backend]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo run build --filter='!@doceu26/desktop'

      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Build chat service
        working-directory: services/chat
        run: GOOS=linux GOARCH=amd64 go build -o chat-service ./cmd/server/

      - name: Build auth service
        working-directory: services/auth
        run: GOOS=linux GOARCH=amd64 go build -o auth-service ./cmd/server/

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/key.pem
          chmod 600 ~/.ssh/key.pem
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy frontend
        run: |
          rsync -avz --delete --rsync-path="sudo rsync" -e "ssh -i ~/.ssh/key.pem" \
            apps/web/dist/ ec2-user@${{ secrets.EC2_HOST }}:/var/www/doceu26/

      - name: Deploy services
        run: |
          scp -i ~/.ssh/key.pem services/chat/chat-service ec2-user@${{ secrets.EC2_HOST }}:/tmp/chat-service
          scp -i ~/.ssh/key.pem services/auth/auth-service ec2-user@${{ secrets.EC2_HOST }}:/tmp/auth-service
          ssh -i ~/.ssh/key.pem ec2-user@${{ secrets.EC2_HOST }} "\
            sudo cp /tmp/chat-service /opt/doceu26/bin/chat-service && \
            sudo cp /tmp/auth-service /opt/doceu26/bin/auth-service && \
            sudo systemctl restart doceu26-chat && \
            sudo systemctl restart doceu26-auth"
